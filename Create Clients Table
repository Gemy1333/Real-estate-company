CREATE TABLE HR.CLIENTS
(
  CLIENT_ID       NUMBER(3),
  CLIENT_NAME     VARCHAR2(20 BYTE),
  CLIENT_ADDRESS  VARCHAR2(20 BYTE),
  CLIENT_NOTES    VARCHAR2(100 BYTE)
);

ALTER TABLE HR.CLIENTS ADD (
  CONSTRAINT CLIENTS_PK
 PRIMARY KEY
 (CLIENT_ID);
