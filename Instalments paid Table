CREATE TABLE HR.INSTALLMENTS_PAID
(
  INSTALLMENT_ID      NUMBER(5),
  CONTRACT_ID         NUMBER(5),
  INSTALLMENT_DATE    DATE,
  INSTALLMENT_AMOUNT  NUMBER(10,2),
  PAID                NUMBER(10)
);

ALTER TABLE HR.INSTALLMENTS_PAID ADD (
  CONSTRAINT INSTALLMENTS_PAID_PK
 PRIMARY KEY
 (INSTALLMENT_ID);
